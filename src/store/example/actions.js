/* import * as types from 'store/example/types';

export const getFiltersRequest = (params) => ({ type: types.GET_FILTERS_REQUEST, params });
export const getFiltersSuccess = (filters) => ({ type: types.GET_FILTERS_SUCCESS, filters });
export const getFiltersError = (error) => ({ type: types.GET_FILTERS_ERROR, error });
*/
