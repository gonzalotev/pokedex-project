/* import http from 'services/http';
import { buildQueryString } from 'utils';

const appService = {
  getFiltersRequest: (params) => http.get(`v1/filters${buildQueryString(params)}`)
};

export default appService;
*/
