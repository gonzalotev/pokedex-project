/* export const getRoles = (state) => state.example.filters?.roles || [];

export const getStates = (state) => state.example.filters?.states || [];

export const getFilters = (state) => state.example.filters;
*/
